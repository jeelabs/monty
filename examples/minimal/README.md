This is a minimal build of Monty with PyVM.

To build and run:

```
$ inv
hello monty minimal
$ inv run ../../pytests/hello.mpy
hello monty minimal
$ inv run ../../pytests/builtins.mpy
{}
12345
{"abc":12345}
$
```
